{
  "name": "@pioneer-sdk/sdk",
  "version": "1.1.68",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "npm": "npm i",
    "npm-update": "npm update",
    "test": "npm run build && node __tests__/test-module.js",
    "test-ts": "rm -R lib && npm run build && ts-node __tests__/test-module-ts.ts",
    "lint": "prettier --write '**/**/*.ts'",
    "start": "nodemon --watch 'src/**/*.ts' --exec ts-node __tests__node",
    "build": "tsc -p .",
    "build:watch": "npm run build && onchange 'src/**/*.ts' -- npm run build",
    "prepublish": "npm run build",
    "refresh": "rm -rf ./node_modules ./package-lock.json && npm install",
    "test-jest": "jest --coverage",
    "test:watch": "jest --coverage --watchAll",
    "view:coverage": "serve coverage/lcov-report",
    "clean": "rm -rf coverage src/**/*.js src/**/*.map lib node_modules"
  },
  "dependencies": {
    "@keepkey/keepkey-sdk": "^0.2.25",
    "@pioneer-platform/loggerdog": "^8.1.18",
    "@pioneer-platform/pioneer-coins": "8.1.61",
    "@pioneer-platform/pioneer-events": "8.1.36",
    "@pioneer-platform/pioneer-invoke": "8.1.36",
    "@pioneer-sdk/tx-builder": "1.0.6",
    "log": "^6.3.1",
    "stable": "^0.1.8",
    "typescript": "4.6.4"
  }
}
